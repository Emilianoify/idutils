# El .env del despliegue: es lo único que distingue una instalación de otra.
# Lleva la contraseña de la base y los secretos de firma de tokens.
.env
.env.*
!.env.example

node_modules/
dist/
*.log
# Local AI runtime state
.atl/

# CodeGraph index: per-checkout, never shared
.codegraph/
